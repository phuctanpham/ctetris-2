% !TEX TS-program = lualatex
% !TEX encoding = UTF-8 Unicode
\documentclass[12pt,a4paper]{article}

\input{src/libs/body.tex}
\input{src/libs/header.tex}
\input{src/libs/footer.tex}
\usepackage{docmute}
\usepackage{lastpage}

\hypersetup{pdftitle={Báo cáo Kỹ năng nghề nghiệp}}
\def\MainDocumentBuildFlag{1}

\setlength{\headheight}{16pt}

\usepackage{titlesec}
\titleformat{\paragraph}[hang]
  {\normalfont\normalsize\bfseries}{\theparagraph}{1em}{}
\titlespacing*{\paragraph}
  {0pt}{3.25ex plus 1ex minus .2ex}{1.5ex plus .2ex}

% ==========================================
% MACRO OVERRIDES FOR MERGED DOCUMENT
% ==========================================
\let\OrigApplyStandardFooter\ApplyStandardFooter

\begin{document}

\hypersetup{pageanchor=false}
\input{src/cover.tex}

% ==========================================
% 1. BẢNG MỤC LỤC CHÍNH (MAIN TOC)
% ==========================================
\clearpage
\setcounter{tocdepth}{1} % Reduced layer outline for main TOC
\input{src/libs/index.tex}

% ==========================================
% 2. BẢNG MỤC LỤC PHỤ LỤC (APPENDIX TOC)
% ==========================================
\vspace{1cm} % Spacing below Main TOC
\section*{Phụ lục}
\makeatletter
\@starttoc{toa} % Reads the custom .toa file generated by appendix.tex
\makeatother

% NOW we clear the page before starting the actual content
\clearpage 

\pagenumbering{arabic}
\setcounter{page}{1}
\hypersetup{pageanchor=true}

% ==========================================
% 3. NỘI DUNG CHÍNH (MAIN CONTENT)
% ==========================================
% FIX: Temporarily redirect the total page count to use the exact last label 
% of content.tex (LastPageReferences) instead of the global document LastPage. 
% This preserves content.tex's footer exactly as if it were compiled alone.
\renewcommand{\ApplyStandardFooter}[2]{%
    \OrigApplyStandardFooter{#1}{LastPageReferences}%
}

\input{src/content.tex}
\clearpage

% FIX: Restore original behavior for appendices so they can use their own 
% independent page counts (e.g., Page 1/2, 2/2).
\let\ApplyStandardFooter\OrigApplyStandardFooter

% FIX: content.tex disabled these macros to prevent inner files from overriding the footer.
% We MUST restore them before processing the appendices so they behave identically to compiling appendix.tex standalone.
\renewcommand{\StandaloneDocumentSetup}[2]{%
  \pagenumbering{arabic}%
  \ApplyStandardFooter{#1}{#2}%
}
\renewcommand{\StandaloneSectionSetup}[2]{%
  \ApplyStandardFooter{#1}{#2}%
}

% ==========================================
% 4. NỘI DUNG PHỤ LỤC (APPENDIX CONTENT)
% ==========================================
% FIX: Place the PDF Viewer bookmark HERE so it shows up at the bottom of the list!
\phantomsection
\pdfbookmark[0]{Phụ lục}{appendix-start}

\appendix
\setlength{\headheight}{16pt}

\IfFileExists{src/appendix.tex}{\input{src/appendix.tex}}{\input{appendix.tex}}

\end{document}