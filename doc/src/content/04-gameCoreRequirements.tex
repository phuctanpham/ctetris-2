% !TEX TS-program = xelatex
% !TEX encoding = UTF-8 Unicode
\documentclass[12pt,a4paper]{article}

\IfFileExists{src/libs/body.tex}{\input{src/libs/body.tex}}{\input{../libs/body.tex}}
\IfFileExists{src/libs/header.tex}{\input{src/libs/header.tex}}{\input{../libs/header.tex}}
\IfFileExists{src/libs/footer.tex}{\input{src/libs/footer.tex}}{\input{../libs/footer.tex}}
\usepackage{docmute}
\usepackage{tikz}
\usetikzlibrary{shapes.geometric, arrows.meta, positioning, calc, fit, backgrounds, trees}
\usepackage{tabularx}
\usepackage{enumitem}

\hypersetup{pdftitle={Software Requirements Specification (SRS) - gameCore Module}}

\begin{document}
\providecommand{\StandaloneSectionSetup}[2]{\ApplyStandardFooter{#1}{#2}}
\StandaloneSectionSetup{04-gameCoreRequirements.tex}{LastPageGameCoreRequirements}
\onehalfspacing

\section{Đặc tả Yêu cầu Nghiệp vụ (SRS): Module gameCore}

\subsection{Tổng quan Nghiệp vụ (Business Overview)}
Phân hệ \textbf{gameCore} là trái tim của ứng dụng \textit{cTetris}, đóng vai trò là Engine xử lý vòng lặp trò chơi (Game Loop) cốt lõi. Module này chịu trách nhiệm mô phỏng vật lý của bàn cờ (Grid), tiếp nhận và xử lý thao tác điều khiển (Input) đa kênh (phím, chuột, vuốt cảm ứng), tính toán va chạm, quản lý điểm số và thời gian chơi.

\subsection{Mô hình hóa Hệ thống (System Modeling)}

\subsubsection{Sơ đồ Ngữ cảnh (System Context Diagram)}
\begin{center}
\begin{tikzpicture}[
    entity/.style={rectangle, draw=black, fill=blue!10, thick, minimum width=2.8cm, minimum height=1.2cm, align=center, font=\small},
    system/.style={circle, draw=orange, fill=orange!10, thick, minimum size=2.8cm, align=center, font=\small},
    arrow/.style={-{Stealth[scale=1.2]}, thick}
]
\node[system] (gamecore) {\textbf{gameCore}};
\node[entity, left=3.5cm of gamecore] (player) {Người chơi};
\node[entity, right=3.5cm of gamecore] (database) {MongoDB \\ (Cloud API)};
\node[entity, above=1.5cm of gamecore] (gameconsole) {Module\\ gameConsole};

\draw[arrow] ($(player.east)+(0,0.3)$) -- node[above, font=\scriptsize, align=center] {Thao tác (Phím/Vuốt)} ($(gamecore.west)+(0,0.3)$);
\draw[arrow] ($(gamecore.west)-(0,0.3)$) -- node[below, font=\scriptsize] {Render Lưới/UI} ($(player.east)-(0,0.3)$);

\draw[arrow] ($(gamecore.east)+(0,0.3)$) -- node[above, font=\scriptsize] {Gửi Điểm \& Tên (V3)} ($(database.west)+(0,0.3)$);
\draw[arrow] ($(database.west)-(0,0.3)$) -- node[below, font=\scriptsize] {Trạng thái lưu} ($(gamecore.east)-(0,0.3)$);

\draw[arrow] ($(gameconsole.south)+(-0.3,0)$) -- node[left, font=\scriptsize] {Khởi tạo Game} ($(gamecore.north)+(-0.3,0)$);
\draw[arrow] ($(gamecore.north)+(0.3,0)$) -- node[right, font=\scriptsize] {Return (Thoát/Menu)} ($(gameconsole.south)+(0.3,0)$);
\end{tikzpicture}
\end{center}

\subsubsection{Sơ đồ Ca sử dụng (Use Case Diagram)}
\begin{center}
\begin{tikzpicture}[node distance=1cm, every node/.style={font=\scriptsize}, actor/.style={circle, draw, fill=gray!10, minimum size=0.5cm}, usecase/.style={ellipse, draw, fill=white, minimum width=2cm}]
    \draw (0,0) rectangle (5,-4); \node at (2.5,-0.3) {\textbf{Hệ thống gameCore}};
    \node (p) [actor] at (-1.5, -2) {Player};
    \node (u1) [usecase] at (2.5,-1) {Di chuyển/Xoay khối};
    \node (u2) [usecase] at (2.5,-1.8) {Rơi nhanh (Soft Drop)};
    \node (u3) [usecase] at (2.5,-2.6) {Tạm dừng (Pause)};
    \node (u4) [usecase] at (2.5,-3.4) {Xem khối kế tiếp};
    \draw [->] (p) -- (u1); \draw [->] (p) -- (u2); 
    \draw [->] (p) -- (u3); \draw [->] (p) -- (u4);
\end{tikzpicture}
\end{center}

\subsubsection{Sơ đồ Hoạt động (Activity Diagram)}
Mô tả luồng hoạt động chi tiết trong một vòng lặp trò chơi (Game Loop).
\begin{center}
\begin{tikzpicture}[
    node distance=1.2cm, 
    every node/.style={font=\scriptsize},
    startstop/.style={rectangle, rounded corners, draw=black, fill=red!10, thick, minimum width=2cm, minimum height=0.6cm, align=center},
    process/.style={rectangle, draw=black, fill=orange!10, thick, minimum width=2.5cm, minimum height=0.6cm, align=center},
    decision/.style={diamond, draw=black, fill=green!10, thick, aspect=2, align=center},
    arrow/.style={-{Stealth[scale=1.2]}, thick}
]
    \node (start) [startstop] {Bắt đầu Ván};
    \node (spawn) [process, below=0.6cm of start] {Sinh khối Tetromino mới};
    \node (input) [process, below=0.6cm of spawn] {Nhận thao tác điều khiển};
    \node (move) [process, below=0.6cm of input] {Cập nhật vị trí/Xoay};
    \node (col) [decision, below=0.6cm of move] {Va chạm?};
    \node (lock) [process, right=1.5cm of col] {Khóa khối \& Xóa dòng};
    \node (over) [decision, above=0.8cm of lock] {Đầy bảng?};
    \node (end) [startstop, right=1.2cm of over] {Game Over};

    \draw [arrow] (start) -- (spawn);
    \draw [arrow] (spawn) -- (input);
    \draw [arrow] (input) -- (move);
    \draw [arrow] (move) -- (col);
    \draw [arrow] (col) -- node[above] {Có} (lock);
    \draw [arrow] (col.west) -- node[above] {Không} ++(-1,0) |- (input.west);
    \draw [arrow] (lock) -- (over);
    \draw [arrow] (over) -- node[above] {Có} (end);
    \draw [arrow] (over.north) |- node[above, near start] {Không} (spawn.east);
\end{tikzpicture}
\end{center}

\subsubsection{Sơ đồ Trạng thái (State Machine Diagram)}
Mô tả các trạng thái vòng đời của module \texttt{gameCore}.
\begin{center}
\begin{tikzpicture}[
    node distance=3cm,
    state/.style={circle, draw=blue, fill=blue!5, thick, minimum size=2cm, align=center, font=\scriptsize},
    arrow/.style={-{Stealth[scale=1.2]}, thick}
]
    \node[state] (init) {INIT};
    \node[state, right=of init] (playing) {PLAYING};
    \node[state, below=1.5cm of playing] (paused) {PAUSED};
    \node[state, right=of playing] (gameover) {GAME\_OVER};

    \draw[arrow] (init) -- node[above, font=\scriptsize] {Khởi tạo xong} (playing);
    \draw[arrow] (playing.250) -- node[left, font=\scriptsize] {Nhấn PAUSE} (paused.110);
    \draw[arrow] (paused.70) -- node[right, font=\scriptsize] {Tiếp tục} (playing.290);
    \draw[arrow] (playing) -- node[above, font=\scriptsize] {Khối chạm đỉnh} (gameover);
    \draw[arrow] (gameover) |- node[near start, below, font=\scriptsize] {Restart} (init);
    \draw[arrow] (paused) -| node[near start, below, font=\scriptsize] {Quit} (gameover);
\end{tikzpicture}
\end{center}

\subsubsection{Cây tính năng (Feature Tree)}
Phân rã cấu trúc các nhóm tính năng cốt lõi của \texttt{gameCore}.
\begin{center}
\begin{tikzpicture}[
    edge from parent fork down,
    level 1/.style={sibling distance=3.5cm, level distance=1.5cm},
    level 2/.style={sibling distance=1.8cm, level distance=1.5cm},
    every node/.style={rectangle, draw=black, rounded corners, fill=gray!10, thick, align=center, font=\scriptsize},
    root/.style={fill=orange!20, font=\small\bfseries, minimum height=0.8cm}
]
\node[root] {Module gameCore}
    child { node[fill=blue!10] {Xử lý Khối}
        child { node {Sinh khối ngẫu nhiên} }
        child { node {Phát hiện va chạm} }
    }
    child { node[fill=green!10] {Điều khiển}
        child { node {Bàn phím} }
        child { node {Chuột (UI)} }
        child { node {Cảm ứng (Vuốt)} }
    }
    child { node[fill=yellow!10] {Luật chơi}
        child { node {Xóa dòng} }
        child { node {Tính điểm \& Combo} }
    }
    child { node[fill=red!10] {Trạng thái \& UI}
        child { node {Đồng hồ đếm} }
        child { node {Popup Tạm dừng} }
    };
\end{tikzpicture}
\end{center}

\subsubsection{Câu chuyện người dùng (Storyboard)}
Mô tả trực quan quá trình trải nghiệm của người chơi từ lúc bắt đầu ván đấu đến khi kết thúc.

\vspace{5pt}
\begin{tabularx}{\linewidth}{|c|X|}
    \hline
    \textbf{Giai đoạn} & \textbf{Mô tả trải nghiệm (User Experience)} \\
    \hline
    \textbf{1. Bắt đầu} & Người chơi vào \texttt{gameCore} từ Menu. Bàn cờ trống rỗng. Khối đầu tiên bắt đầu rơi chậm từ đỉnh màn hình. Âm nhạc nổi lên. Ở góc phải (Sidebar), người chơi thấy điểm số là 00000, đồng hồ 00:00 và hình dáng khối sắp tới. \\
    \hline
    \textbf{2. Xử lý} & Người chơi dùng phím mũi tên hoặc vuốt màn hình để dịch chuyển khối sang trái/phải và lật khối cho khớp với các rãnh trống phía dưới. Khi chắc chắn, người chơi nhấn SPACE để khối rơi nhanh (Soft Drop) xuống đáy. \\
    \hline
    \textbf{3. Ăn điểm} & Khối lấp đầy 2 hàng ngang liền kề. Màn hình chớp nháy nhẹ, 2 hàng biến mất kèm âm thanh "Clear". Các khối bên trên tụt xuống. Điểm số trên Sidebar tăng vọt nhờ hiệu ứng nhân Combo. Tốc độ rơi của khối mới bắt đầu tăng lên một chút. \\
    \hline
    \textbf{4. Tạm dừng} & Người chơi có việc bận, bấm nút PAUSE (hoặc phím ENTER). Bàn cờ bị phủ mờ tối lại, nhạc nền tạm dừng, xuất hiện popup báo hiệu trạng thái dừng. \\
    \hline
    \textbf{5. Game Over} & Khi các khối chất đống cao chạm đến mép trên cùng, trò chơi dừng lại. Một popup lớn xuất hiện thông báo GAME OVER, tổng kết điểm và thời gian, đồng thời cho phép nhập tên để lưu lên Bảng xếp hạng. \\
    \hline
\end{tabularx}

\subsection{Yêu cầu Phiên bản V1: Core Gameplay \& Controls}
\begin{itemize}[leftmargin=*]
    \item \textbf{Mục tiêu cốt lõi:} Xây dựng hệ thống vật lý và logic bàn cờ vững chắc cho game xếp hình, hỗ trợ thao tác đa nền tảng (Desktop/WASM) và đa thiết bị.
    \item \textbf{Yêu cầu Chức năng:}
    \begin{itemize}[noitemsep]
        \item Sinh ngẫu nhiên 5 khối (L, I, Z, O, T) tích hợp 50\% xác suất lật gương (Mirror Flip).
        \item Điều khiển qua Phím (WASD/Arrows), Chuột (Click \& Hold Sidebar), và Cảm ứng (Vuốt $\geq$ 15px trục ưu thế).
        \item Đồng hồ tính giờ chơi thực tế (loại trừ thời gian Pause) định dạng HH:MM. Điểm số tối đa 99999.
    \end{itemize}
    \item \textbf{Yêu cầu Phi Chức năng:} Đảm bảo thao tác sát tường hoặc kẹt khối tự động bị hủy để không làm vỡ logic mảng lưới. Frame rate ổn định ở 60 FPS.
    \item \textbf{Yêu cầu Kỹ thuật \& Tiêu chí thành công:}
    \begin{itemize}[noitemsep]
        \item Kỹ thuật: Không cấp phát bộ nhớ động (RAM) liên tục trong Game Loop. Lưới ma trận tĩnh.
        \item \textbf{Tiêu chí thành công:} Khối di chuyển, lật, rớt và ăn điểm chính xác. Cử chỉ vuốt nhận diện chuẩn xác.
    \end{itemize}
\end{itemize}

% ============================================================
% PATCH V2 — insert after V1 subsection, before V3 subsection
% in 04-gameCoreRequirements.tex
% ============================================================

\subsection{Yêu cầu Phiên bản V2: Tích hợp Dữ liệu \& Nâng cao Gameplay}

\subsubsection{Tổng quan nâng cấp so với V1}

\begin{tabularx}{\linewidth}{|l|X|X|}
    \hline
    \textbf{Khía cạnh} & \textbf{V1 (baseline)} & \textbf{V2 (delta)} \\
    \hline
    Tốc độ rơi & Hằng số 500ms & Hàm động theo điểm: 5 mốc 500→400→300→200→100ms \\
    \hline
    Xóa dòng & Tức thì, không hiệu ứng & Flash 3 lần (6 frame × 80ms) trước khi xóa thật \\
    \hline
    Tính điểm & 1 dòng = 1 điểm & Combo $n^2$: 2 dòng=4pt, 3=9pt, 4=16pt \\
    \hline
    Khối preview & NEXT-1 cố định & Queue 3 khối; NEXT-2/3 unlock theo ngưỡng điểm story \\
    \hline
    Màu khối & Random 6 màu cứng & Từ \texttt{cfg.colorEnabled[7]} (Console Settings) \\
    \hline
    Bàn cờ khởi đầu & Trống hoàn toàn & \texttt{cfg.tableMatrix} pre-fill từ \texttt{shared\_data} \\
    \hline
    Lưu thành tích & Không & \texttt{dbInsertRecord()} + \texttt{dbUpsertStoryProgress()} \\
    \hline
    Âm thanh & Stub log & \texttt{SDL\_AudioStream} + gain từ \texttt{cfg.volume} \\
    \hline
    Thông tin story & Không & Label \texttt{"S\{id\}-C\{id\}"} trên sidebar slot 4 \\
    \hline
\end{tabularx}

% ---- Sơ đồ Trạng thái mở rộng V2 ----
\subsubsection{Sơ đồ Trạng thái V2 (State Machine — mở rộng)}

\noindent V2 bổ sung trạng thái \textsc{Flash\_Clear} (không thể interrupt) và luồng ghi DB (\textsc{Persist}) tại mỗi điểm thoát.

\begin{center}
\begin{tikzpicture}[
    node distance=2.6cm,
    state/.style={circle, draw=blue!60, fill=blue!5, thick,
                  minimum size=1.9cm, align=center, font=\scriptsize},
    persist/.style={rectangle, draw=green!60, fill=green!5, thick, rounded corners,
                    minimum width=2.0cm, minimum height=0.7cm,
                    align=center, font=\scriptsize},
    arrow/.style={-{Stealth[scale=1.1]}, thick},
    darrow/.style={-{Stealth[scale=1.1]}, thick, dashed, gray}
]
    \node[state] (init)     {INIT};
    \node[state, right=of init]  (play)    {PLAYING};
    \node[state, below=1.5cm of play]  (flash)   {FLASH\\CLEAR};
    \node[state, above right=1.2cm and 2.0cm of play] (pause)   {PAUSED};
    \node[state, below right=1.2cm and 2.0cm of play] (over)    {GAME\\OVER};
    \node[state, right=3.0cm of over] (wasm)   {WASM\\RELOAD};

    \node[persist, below=0.9cm of flash] (db) {\texttt{dbInsert}\\\texttt{Record()}};
    \node[persist, right=0.8cm of over]  (db2) {\texttt{dbUpsert}\\\texttt{Progress()}};

    % transitions
    \draw[arrow] (init) -- node[above,font=\scriptsize]{entry} (play);
    \draw[arrow] (play.south) -- node[left,font=\scriptsize]{row full} (flash.north);
    \draw[arrow] (flash.west) to[bend right=40]
        node[left,font=\scriptsize]{6 frames\\done} (play.south west);
    \draw[arrow] (play.north east) to[bend left=15]
        node[above,font=\scriptsize]{PAUSE/ENTER} (pause.south west);
    \draw[arrow] (pause.south) to[bend left=15]
        node[below,font=\scriptsize]{resume} (play.north);
    \draw[arrow] (play.south east) to[bend right=15]
        node[below,font=\scriptsize]{block at top} (over.north west);
    \draw[arrow] (over) -- node[above,font=\scriptsize]{Quit\\(WASM)} (wasm);
    \draw[arrow] (over.south) -- ++(0,-0.5) -| node[below,font=\scriptsize]{Restart} (init.south);
    \draw[arrow] (over.north east) -- ++(0.4,0.4)
        node[above right,font=\scriptsize,draw=blue!30,fill=white,rounded corners,inner sep=2pt]
        {ret 2 → Console};

    % DB persist edges
    \draw[darrow] (flash.south) -- (db.north);
    \draw[darrow] (over.east) -- (db2.west);
    \draw[darrow] (db2.south) -- ++(0,-0.4)
        node[below,font=\tiny]{\texttt{dbCheckAndUnlockStories()}};
\end{tikzpicture}
\end{center}

% ---- Luồng dữ liệu SettingsConfig ----
\subsubsection{Luồng dữ liệu \texttt{SettingsConfig} V2 (Data Flow)}

\noindent \texttt{SettingsConfig} mở rộng thêm 3 fields ở V2; gameCore đọc toàn bộ nhưng không ghi ngược.

\begin{center}
\begin{tikzpicture}[
    every node/.style={font=\scriptsize},
    box/.style={rectangle, draw=black, thick, fill=white, inner sep=5pt, align=left},
    arrow/.style={-{Stealth[scale=1.1]}, thick},
    darrow/.style={-{Stealth[scale=1.1]}, thick, dashed}
]
    \node[box] (cons) at (0,0) {
        \textbf{gameConsole}\\
        \rule{3.2cm}{0.4pt}\\
        Settings popup\\
        Stories popup\\
        \texttt{dbLoadSettings()}\\
        \texttt{dbSelectStory()}
    };
    \node[box, right=2.0cm of cons] (cfg) {
        \textbf{SettingsConfig} \textit{(V2)}\\
        \rule{3.8cm}{0.4pt}\\
        \texttt{float  volume}\\
        \texttt{bool   colorEnabled[7]}\\
        \texttt{int    storyId / chapterId}\\
        \textcolor{red}{\texttt{int    nextBlockScore} \textit{(new)}}\\
        \textcolor{red}{\texttt{float  nextBlockSpeed} \textit{(new)}}\\
        \textcolor{red}{\texttt{string tableMatrix} \textit{(new)}}
    };
    \node[box, right=2.0cm of cfg] (core) {
        \textbf{gameCore}\\
        \rule{3.2cm}{0.4pt}\\
        \texttt{coreOpenBgm(volume)}\\
        \texttt{spawnBlock(colorEnabled)}\\
        \texttt{applyTableMatrix(tm)}\\
        NEXT-2/3 unlock\\
        \texttt{dbInsertRecord()}
    };

    \draw[arrow] (cons.east) -- node[above,font=\scriptsize]{mutate} (cfg.west);
    \draw[arrow] (cfg.east)  -- node[above,font=\scriptsize]{read-only} (core.west);

    \draw[darrow, blue!60] (cfg.south) -- ++(0,-0.6)
        node[below,draw=blue!30,fill=blue!5,rounded corners,inner sep=2pt]
        {\texttt{dbSaveSettings()} / \texttt{dbLoadSettings()}};
\end{tikzpicture}
\end{center}

% ---- DB Lifecycle ----
\subsubsection{Vòng đời Kết nối SQLite trong gameCore V2}

\noindent\textbf{Nguyên tắc:} gameCore \emph{không sở hữu} DB connection. Mỗi game-over event mở/đóng connection trong cùng một transaction block. Console tự mở lại khi re-enter.

\begin{center}
\begin{tikzpicture}[
    every node/.style={font=\scriptsize},
    proc/.style={rectangle, draw=black, fill=orange!10, thick,
                 minimum width=4.0cm, minimum height=0.55cm, align=center},
    dec/.style={diamond, draw=black, fill=green!10, thick,
                aspect=2.2, align=center},
    term/.style={rectangle, rounded corners, draw=black, fill=red!10, thick,
                 minimum width=3.2cm, minimum height=0.55cm, align=center},
    arrow/.style={-{Stealth[scale=1.1]}, thick},
    note/.style={rectangle, draw=gray, dashed, fill=yellow!5,
                 rounded corners, inner sep=3pt, font=\tiny}
]
    \node[term]  (go)   {isGameOver = true};
    \node[proc,  below=0.5cm of go]   (open) {\texttt{dbOpen("default")}};
    \node[dec,   below=0.5cm of open] (ok)   {open\\success?};
    \node[proc,  below=0.5cm of ok]   (ins)  {\texttt{dbInsertRecord(rec)}};
    \node[proc,  below=0.5cm of ins]  (ups)  {\texttt{dbUpsertStoryProgress(...)}};
    \node[proc,  below=0.5cm of ups]  (unl)  {\texttt{dbCheckAndUnlockStories("default")}};
    \node[proc,  below=0.5cm of unl]  (cls)  {\texttt{dbClose()}};
    \node[term,  below=0.5cm of cls]  (end)  {gameOverRecorded = true};

    \node[note, right=1.5cm of ins]  {startTS, endTS, score,\\storyId, chapterId,\\avgSpeed, retryNo};
    \node[note, right=1.5cm of unl]  {cascade unlock\\per requiredStories CSV};
    \node[note, left=1.5cm of ok]    {log warn,\\skip record};

    \draw[arrow] (go) -- (open);
    \draw[arrow] (open) -- (ok);
    \draw[arrow] (ok) -- node[left]{yes} (ins);
    \draw[arrow] (ok.west) -- ++(-1.5,0) |- node[right,pos=0.1]{no} (end.west);
    \draw[arrow] (ins) -- (ups);
    \draw[arrow] (ups) -- (unl);
    \draw[arrow] (unl) -- (cls);
    \draw[arrow] (cls) -- (end);
\end{tikzpicture}
\end{center}

% ---- Scoring \& Speed diagram ----
\subsubsection{Hệ thống Điểm số và Tốc độ V2}

\begin{center}
\begin{tikzpicture}[every node/.style={font=\scriptsize}]
% Speed curve
\begin{scope}
    \draw[->] (0,0) -- (6.2,0) node[right] {Score};
    \draw[->] (0,0) -- (0,3.2) node[above] {Fall interval (ms)};

    % step function
    \draw[blue!70, very thick]
        (0,2.5)   -- (0.8,2.5)
        (0.8,2.0) -- (1.5,2.0)
        (1.5,1.5) -- (2.3,1.5)
        (2.3,1.0) -- (3.5,1.0)
        (3.5,0.5) -- (6.0,0.5);

    % tick labels x
    \foreach \x/\lbl in {0/0, 0.8/5, 1.5/15, 2.3/30, 3.5/50} {
        \draw (\x,0.05) -- (\x,-0.05) node[below] {\lbl};
    }
    % tick labels y
    \foreach \y/\lbl in {2.5/500, 2.0/400, 1.5/300, 1.0/200, 0.5/100} {
        \draw (0.05,\y) -- (-0.05,\y) node[left] {\lbl};
    }

    % soft drop = /5 reference
    \draw[red!60, dashed, thin]
        (0,0.5)   -- (0.8,0.5)
        (0.8,0.4) -- (1.5,0.4)
        (1.5,0.3) -- (2.3,0.3)
        (2.3,0.2) -- (3.5,0.2)
        (3.5,0.1) -- (6.0,0.1);
    \node[red!60] at (5.5,0.3) {Soft Drop (÷5)};
    \node[blue!70] at (5.0,1.2) {Normal};
\end{scope}

% Combo table
\begin{scope}[xshift=7.5cm]
    \node[draw=black, fill=gray!10, thick, minimum width=3.5cm,
          minimum height=0.5cm, align=center] at (1.75,3.0) {\textbf{Combo Score ($n^2$)}};
    \foreach \row/\lines/\pts/\clr in {
        0/1 line/1 pt/white,
        1/2 lines/4 pts/yellow!10,
        2/3 lines/9 pts/orange!10,
        3/4 lines/16 pts/red!10
    }{
        \node[draw=black, fill=\clr, minimum width=1.6cm,
              minimum height=0.45cm, align=center]
            at (0.8, 2.4 - \row*0.5) {\lines};
        \node[draw=black, fill=\clr, minimum width=1.6cm,
              minimum height=0.45cm, align=center]
            at (2.7, 2.4 - \row*0.5) {\pts};
    }
    \node[font=\tiny] at (1.75, 0.35) {cap: 99999};
\end{scope}
\end{tikzpicture}
\end{center}

% ---- Unlock Logic ----
\subsubsection{Logic mở khóa NEXT-2 / NEXT-3 (Story-gated Preview)}

\begin{center}
\begin{tikzpicture}[
    every node/.style={font=\scriptsize},
    cond/.style={diamond, draw=black, fill=green!10, thick, aspect=2.5, align=center},
    slot/.style={rectangle, draw=black, fill=blue!10, thick,
                 minimum width=1.8cm, minimum height=0.6cm, align=center},
    arrow/.style={-{Stealth[scale=1.1]}, thick}
]
    \node[slot] (n1) at (0,0)    {NEXT-1\\(always)};
    \node[cond] (c2) at (4,0)    {\texttt{cfg.nextBlock}\\\texttt{Score > 0}\\AND\\score $\geq$ threshold};
    \node[slot] (n2) at (8,0)    {NEXT-2\\(unlocked)};
    \node[cond] (c3) at (4,-2.0) {NEXT-2 open\\AND\\score $\geq$ threshold×2};
    \node[slot] (n3) at (8,-2.0) {NEXT-3\\(unlocked)};
    \node[slot, draw=gray, fill=gray!10] (n2h) at (8,-1.0) {NEXT-2\\(hidden)};
    \node[slot, draw=gray, fill=gray!10] (n3h) at (8,-3.0) {NEXT-3\\(hidden)};

    \draw[arrow] (n1.east) -- (c2.west);
    \draw[arrow] (c2.east) -- node[above]{yes} (n2.west);
    \draw[arrow] (c2.south) -- node[right]{no} (n2h);
    \draw[arrow] (c2.south) -- (c3.north);
    \draw[arrow] (c3.east) -- node[above]{yes} (n3.west);
    \draw[arrow] (c3.south) -- node[right]{no} (n3h);

    \node[font=\tiny, draw=orange!40, fill=orange!5, rounded corners,
          inner sep=2pt, below=0.3cm of n3]
        {TODO(V3): add \texttt{nextBlockScore3} field to SettingsConfig};
\end{tikzpicture}
\end{center}

% ---- Flash Effect Sequence ----
\subsubsection{Chuỗi Hiệu ứng Flash khi Xóa dòng (Timing Diagram)}

\begin{center}
\begin{tikzpicture}[every node/.style={font=\scriptsize}, x=1.1cm, y=0.7cm]
    % timeline axis
    \draw[->] (0,0) -- (8.5,0) node[right] {time (ms)};

    % tick marks every ~80ms (1 unit = 80ms)
    \foreach \x in {0,1,...,8} {
        \draw (\x, 0.1) -- (\x, -0.1) node[below] {\x×80};
    }

    % flash state bar
    \foreach \x/\c in {0/white!80!black, 1/black!80, 2/white!80!black,
                        3/black!80, 4/white!80!black, 5/black!80} {
        \fill[\c] (\x, 0.3) rectangle (\x+1, 0.8);
        \draw[black] (\x, 0.3) rectangle (\x+1, 0.8);
    }
    \node[left] at (0, 0.55) {Flash row};

    % fall blocked bar
    \fill[red!20] (0, 1.0) rectangle (6, 1.5);
    \draw[red!60] (0, 1.0) rectangle (6, 1.5);
    \node[left] at (0, 1.25) {Fall blocked};

    % actual clear
    \draw[green!60!black, very thick] (6, -0.4) -- (6, 1.6);
    \node[green!60!black, below] at (6, -0.5) {Lines cleared};

    % labels
    \node at (3, 0.55) {\tiny flashTick 0→5};
    \node at (3, 1.25) {\tiny pendingClear = true};
\end{tikzpicture}
\end{center}

% ---- tableMatrix format ----
\subsubsection{Định dạng \texttt{tableMatrix} (Board Pre-population)}

\noindent Format chuẩn V2: \texttt{";"-separated rows (bottom-aligned), ","-separated colorID values (0=empty, 1-6=colors)}.

\begin{center}
\begin{tikzpicture}[every node/.style={font=\scriptsize}]
    % board grid (10×4 bottom rows)
    \foreach \r in {0,1,2,3} {
        \foreach \c in {0,...,9} {
            \draw[gray!50] (\c*0.4, \r*0.4) rectangle (\c*0.4+0.4, \r*0.4+0.4);
        }
    }
    % pre-filled row 0 (bottom = row index 18 in board)
    \foreach \c/\clr in {2/red!60, 5/blue!60, 8/green!60} {
        \fill[\clr] (\c*0.4+0.05, 0.05) rectangle (\c*0.4+0.35, 0.35);
    }
    % pre-filled row 1
    \foreach \c/\clr in {0/orange!70, 3/purple!60, 6/yellow!70, 9/red!60} {
        \fill[\clr] (\c*0.4+0.05, 0.45) rectangle (\c*0.4+0.35, 0.75);
    }

    % annotation
    \node[right] at (4.2, 0.2) {\texttt{"0,0,2,0,0,1,0,0,3,0"} ← row N-1 (near bottom)};
    \node[right] at (4.2, 0.6) {\texttt{"4,0,0,5,0,0,6,0,0,2"} ← row N (bottom)};
    \node[right] at (4.2, 1.2) {rows joined by \texttt{";"},};
    \node[right] at (4.2, 0.95) {empty string → blank board};

    \draw[->] (4.1, 0.2) -- (3.8, 0.2);
    \draw[->] (4.1, 0.6) -- (3.8, 0.6);
\end{tikzpicture}
\end{center}

\subsubsection{Yêu cầu Chức năng V2}
\begin{itemize}[leftmargin=*]
    \item \textbf{BGM + Volume (C1):} \texttt{SDL\_OpenAudioDeviceStream} + \texttt{SDL\_SetAudioStreamGain(cfg.volume)} khi entry; \texttt{SDL\_DestroyAudioStream} khi exit. Không có audio device → graceful no-op.
    \item \textbf{Tốc độ động (C2):} \texttt{getFallInterval(score)} 5-step function; softDrop = \texttt{interval/5}. Recompute mỗi frame (không cache).
    \item \textbf{3-block queue (C3):} \texttt{nextBlock2/3} trong \texttt{GameState}; \texttt{lockBlock()} duy trì queue; NEXT-2/3 hiển thị theo ngưỡng \texttt{cfg.nextBlockScore}.
    \item \textbf{Flash clear (C4):} \texttt{pendingClear=true} → 6 frame × 80ms → \texttt{finishLineClear()}; fall loop tạm dừng trong phase này.
    \item \textbf{Combo score (C5):} \texttt{score += clearRowCount²}; cap 99999.
    \item \textbf{Color palette (C6):} \texttt{spawnBlock()} build \texttt{enabledIndices[]} từ \texttt{cfg.colorEnabled[7]}; fallback toàn palette nếu mảng rỗng.
    \item \textbf{tableMatrix (C7):} \texttt{applyTableMatrix()} parse trong \texttt{resetGame()} TRƯỚC \texttt{spawnBlock()}; parse lỗi → blank board, không crash.
    \item \textbf{DB write (C8/C9):} \texttt{onGameOver()} — \texttt{dbOpen} → \texttt{dbInsertRecord} → \texttt{dbUpsertStoryProgress} → \texttt{dbCheckAndUnlockStories} → \texttt{dbClose}; guard \texttt{gameOverRecorded} tránh duplicate.
\end{itemize}

\subsubsection{Yêu cầu Phi Chức năng V2}
\begin{itemize}[noitemsep, leftmargin=*]
    \item \textbf{DB lifecycle:} gameCore không sở hữu connection; mở/đóng trong cùng 1 transaction block. Console tự mở lại DB của nó khi re-enter — không double-open.
    \item \textbf{WASM:} \texttt{EM\_ASM(\{Module.FS.syncfs(false,function()\{\}\})} sau mỗi \texttt{dbInsertRecord()}.
    \item \textbf{Flash effect:} không block event polling; \texttt{SDL\_PollEvent} vẫn chạy trong phase flash.
    \item \textbf{tableMatrix:} parse lỗi phải log \texttt{warning} và tiếp tục — không gây crash hay undefined board state.
\end{itemize}

\subsubsection{Tiêu chí Thành công V2}
\begin{tabularx}{\linewidth}{|l|X|}
    \hline
    \textbf{Kịch bản kiểm thử} & \textbf{Kết quả mong đợi} \\
    \hline
    score=0 → score=50 & Fall interval giảm từ 500ms → 100ms; soft drop luôn = interval÷5 \\
    \hline
    Xóa 4 dòng cùng lúc & Flash 3 lần → score tăng đúng 16 điểm \\
    \hline
    Console: tắt 5 màu, giữ 2 màu & Mọi khối sinh ra chỉ có 2 màu đó \\
    \hline
    Story có tableMatrix 2 dòng & Board bắt đầu với 2 dòng pre-filled; blank board nếu empty string \\
    \hline
    Story 1 (nextBlockScore=50), score<50 & NEXT-2 ẩn; score≥50 → NEXT-2 hiện \\
    \hline
    Game over → query DB & \texttt{SELECT COUNT(*)} tăng 1; \texttt{endTS - startTS ≥ elapsedMs÷1000} \\
    \hline
    Quit → Console → Board popup & sync\_Records có record mới; leaderboard refresh \\
    \hline
    WASM: game over → đóng tab → mở lại & Record vẫn còn trong \texttt{idUser\_Records} qua IDBFS \\
    \hline
\end{tabularx}

% ---- V3 mention update ----
\subsubsection*{\small Lưu ý hướng V3}
\noindent V3 thay thế \texttt{nextBlockScore × 2} fallback bằng field \texttt{nextBlockScore3} riêng trong \texttt{SettingsConfig}. Lưu trữ thành tích chuyển từ write-local sang Cloudflare D1 (1-way sync qua Cloudflare Worker proxy); SQLite vẫn là nguồn \textit{source of truth} cục bộ — D1 là bản sao read-only phục vụ leaderboard toàn cầu.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.55\textwidth]{04-gameCoreRequirements-v2-screenshot}
    \\[6pt]
    \textit{\small Hình V2.1: gameCore V2 — sidebar hiển thị S2-C1, NEXT-2 đã unlock, bàn cờ có tableMatrix pre-fill.}
\end{figure}

% ============================================================
% END PATCH V2 — 04-gameCoreRequirements.tex
% ============================================================

\subsection{Yêu cầu Phiên bản V3: Cạnh tranh \& Lưu trữ Đám mây}
\begin{itemize}[leftmargin=*]
    \item \textbf{Mục tiêu cốt lõi:} Số hóa thành tích thông qua việc cho phép người chơi định danh và lưu trữ điểm số vinh danh lên hệ thống Server Cloud.
    \item \textbf{Yêu cầu Chức năng:}
    \begin{itemize}[noitemsep]
        \item Bắt phím (Keyboard Event) để người dùng gõ tên trực tiếp trên màn hình Game Over.
        \item Đóng gói Payload (Tên, Điểm, Thời gian) và gọi API đẩy dữ liệu lên cơ sở dữ liệu MongoDB Atlas.
    \end{itemize}
    \item \textbf{Yêu cầu Phi Chức năng:} Tác vụ gọi mạng không được chặn vòng lặp chính của ứng dụng. Đảm bảo bảo mật chuỗi định dạng.
    \item \textbf{Yêu cầu Kỹ thuật \& Tiêu chí thành công:}
    \begin{itemize}[noitemsep]
        \item Kỹ thuật: C++ HTTP Request (\texttt{curl} cho Native, \texttt{emscripten\_fetch} cho Web).
        \item \textbf{Tiêu chí thành công:} Dữ liệu xuất hiện chính xác trên MongoDB Atlas.
    \end{itemize}
\end{itemize}

\label{LastPageGameCoreRequirements}
\end{document}
