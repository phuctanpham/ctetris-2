name: Deploy WASM to GitHub Pages

on:
  push:
    branches:
      - main
      - test
    paths:
      - 'app/src/**'
      - 'app/main.cpp'
      - 'app/web/**'
      - 'app/brandkit/**'
      - 'app/CMakeLists.txt'
      - 'app/build.sh'
      - 'app/build.ps1'
      - '.github/workflows/deploy-pages.yml'
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  BUILD_OS: ubuntu   # ← Only line to change: 'ubuntu' | 'windows'

# Updated permissions to ensure GitHub Actions can write the deployment
permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  # ---------------------------------------------------------------------------
  # Job 0: CONFIG  (env context is forbidden in runs-on; derive runner here
  #                 and expose it via needs.config.outputs.runner)
  # ---------------------------------------------------------------------------
  config:
    name: Resolve runner
    runs-on: ubuntu-latest
    outputs:
      runner: ${{ steps.pick.outputs.runner }}
    steps:
      - id: pick
        shell: bash
        run: |
          if [ "${{ env.BUILD_OS }}" = "windows" ]; then
            echo "runner=windows-latest" >> $GITHUB_OUTPUT
          else
            echo "runner=ubuntu-latest" >> $GITHUB_OUTPUT
          fi

  # ---------------------------------------------------------------------------
  # Job 1: BUILD
  # ---------------------------------------------------------------------------
  build:
    name: Build WASM
    needs: config
    runs-on: ${{ needs.config.outputs.runner }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Cache heavy downloads -- path and key both driven by BUILD_OS
      - name: Cache heavy downloads
        uses: actions/cache@v4
        with:
          path: app/libs/${{ env.BUILD_OS }}/downloads
          key: libs-${{ env.BUILD_OS }}-${{ hashFiles('app/build.sh', 'app/build.ps1') }}-${{ runner.os }}
          restore-keys: |
            libs-${{ env.BUILD_OS }}-

      # ---------- Ubuntu setup ----------
      - name: Install build tools (Ubuntu)
        if: env.BUILD_OS == 'ubuntu'
        run: sudo apt-get install -y ninja-build

      # ---------- Windows setup ----------
      - name: Install Ninja (Windows)
        if: env.BUILD_OS == 'windows'
        shell: pwsh
        run: choco install ninja -y --no-progress

      # ===================================================================
      # Build WASM -- Ubuntu
      # emsdk bootstrap is inlined in the same run block so the sourced
      # PATH persists for the build.sh wasm call (separate steps lose it).
      # ===================================================================
      - name: Build WASM (Ubuntu)
        if: env.BUILD_OS == 'ubuntu'
        working-directory: app
        shell: bash
        run: |
          EMSDK_DIR="libs/ubuntu/downloads/emsdk"
          mkdir -p "libs/ubuntu/downloads"
          [ -d "$EMSDK_DIR" ] || git clone https://github.com/emscripten-core/emsdk.git "$EMSDK_DIR"
          (cd "$EMSDK_DIR" && ./emsdk install 3.1.72 && ./emsdk activate 3.1.72)
          source "$EMSDK_DIR/emsdk_env.sh"
          bash build.sh wasm 2>&1 | tee build.log
          exit ${PIPESTATUS[0]}

      # ===================================================================
      # Build WASM -- Windows
      # ===================================================================
      - name: Build WASM (Windows)
        if: env.BUILD_OS == 'windows'
        working-directory: app
        shell: pwsh
        run: |
          .\build.ps1 wasm 2>&1 | Tee-Object -FilePath build.log
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      # Upload build log for debugging
      - name: Upload build log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-log
          path: app/build.log
          retention-days: 7

      # Stage Pages artifact
      # bash is available on both ubuntu-latest and windows-latest (Git Bash),
      # so a single shell: bash step works for both runners.
      # Output dir is app/build/wasm/<BUILD_OS>/ in both build scripts.
      - name: Stage Pages artifact
        shell: bash
        run: |
          mkdir -p public
          src="app/build/wasm/${{ env.BUILD_OS }}"
          cp "$src/cTetris.html" public/index.html
          cp "$src/cTetris.js"   public/
          cp "$src/cTetris.wasm" public/
          for asset in favicon.svg favicon.ico manifest.webmanifest sw.js; do
            if [ -f "$src/$asset" ]; then
              cp "$src/$asset" "public/$asset"
              echo "[OK] $asset"
            else
              echo "[WARN] $asset not in build output -- PWA may be affected"
            fi
          done
          touch public/.nojekyll
          echo "--- public/ contents ---"
          ls -la public/

      # Upload Pages artifact
      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: public

  # ---------------------------------------------------------------------------
  # Job 2: DEPLOY GITHUB PAGES
  # deploy-pages action only runs on ubuntu -- runner is always ubuntu-latest
  # regardless of which OS was used to build.
  # ---------------------------------------------------------------------------
  deploy:
    name: Deploy to GitHub Pages
    needs: [config, build]
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    permissions:
      contents: write
      pages: write
      id-token: write
      actions: write        # required to list + delete artifacts
    steps:
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4

      - name: Cleanup workflow artifacts (except cache)
        if: ${{ success() }}
        shell: bash
        run: |
          ids=$(gh api "repos/${{ github.repository }}/actions/artifacts" \
            --paginate -q '.artifacts[] | select(.expired==false) | select(.name | test("^cache") | not) | .id' 2>/dev/null || true)
          if [ -z "$ids" ]; then
            echo "No artifacts to delete."
            exit 0
          fi
          echo "$ids" | xargs -r -I{} gh api --method DELETE \
            "repos/${{ github.repository }}/actions/artifacts/{}"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}